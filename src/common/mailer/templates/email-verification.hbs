<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <meta name="color-scheme" content="light">
  <meta name="supported-color-schemes" content="light">
  <title>Verify Your Email — ID Verification Platform</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:opsz,wght@9..40,400;9..40,500;9..40,600;9..40,700&display=swap" rel="stylesheet">
  <style>
    body {
      margin: 0;
      padding: 0;
      background-color: #ede9ff;
      font-family: 'DM Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Helvetica Neue', Arial, sans-serif;
    }
    .wrapper { padding: 48px 20px; }
    .card {
      max-width: 580px;
      margin: 0 auto;
      background: #ffffff;
      border-radius: 20px;
      overflow: hidden;
      box-shadow: 0 4px 32px rgba(91,35,255,0.13), 0 1px 4px rgba(0,0,0,0.06);
    }
    .header {
      background-color: #5B23FF;
      padding: 44px 48px 40px;
      text-align: center;
    }
    .brand-label {
      font-size: 11px;
      font-weight: 600;
      color: rgba(255,255,255,0.52);
      letter-spacing: 2px;
      text-transform: uppercase;
      margin: 0 0 22px;
    }
    .header h1 {
      color: #ffffff;
      font-size: 26px;
      font-weight: 700;
      margin: 18px 0 8px;
      letter-spacing: -0.4px;
    }
    .header-sub {
      color: rgba(255,255,255,0.70);
      font-size: 14px;
      margin: 0;
    }
    .body { padding: 44px 48px 40px; }
    .greeting {
      font-size: 18px;
      font-weight: 600;
      color: #16103a;
      margin: 0 0 14px;
    }
    .text {
      font-size: 15px;
      color: #5a5380;
      line-height: 1.75;
      margin: 0 0 36px;
    }
    .expires {
      text-align: center;
      font-size: 13px;
      color: #9590b0;
      margin: 0 0 32px;
    }
    .divider {
      border: none;
      border-top: 1px solid #ede9ff;
      margin: 0 0 24px;
    }
    .fallback {
      font-size: 12px;
      color: #9590b0;
      line-height: 1.7;
      word-break: break-all;
      margin: 0;
    }
    .fallback a { color: #5B23FF; text-decoration: none; }
    .footer {
      background: #f5f3ff;
      border-top: 1px solid #ede9ff;
      padding: 24px 48px;
      text-align: center;
    }
    .footer p {
      font-size: 12px;
      color: #9590b0;
      margin: 0 0 4px;
      line-height: 1.65;
    }
    .footer p:last-child { margin: 0; }
  </style>
</head>
<body>
  <!-- Preheader -->
  <div style="display:none;font-size:1px;line-height:1px;max-height:0;max-width:0;opacity:0;overflow:hidden;">
    Please verify your email address to activate your account.
  </div>

  <div class="wrapper">
    <div class="card">

      <!-- Header -->
      <div class="header">
        <p class="brand-label">ID Verification Platform</p>
        <table role="presentation" cellpadding="0" cellspacing="0" style="margin: 0 auto;">
          <tr>
            <td width="72" height="72" align="center" valign="middle"
                style="width:72px;height:72px;background:rgba(255,255,255,0.15);border:1.5px solid rgba(255,255,255,0.28);border-radius:36px;">
              <svg width="30" height="30" viewBox="0 0 30 30" fill="none" xmlns="http://www.w3.org/2000/svg">
                <rect x="3" y="7" width="24" height="17" rx="2.5" fill="rgba(255,255,255,0.18)" stroke="white" stroke-width="1.5"/>
                <path d="M3 10.5l12 8.5 12-8.5" stroke="white" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </td>
          </tr>
        </table>
        <h1>Verify Your Email</h1>
        <p class="header-sub">One step away from activating your account</p>
      </div>

      <!-- Body -->
      <div class="body">
        <p class="greeting">Hello, {{postNames}} {{surName}}</p>
        <p class="text">
          Thank you for registering with the ID Verification Platform. To complete
          your registration and activate your account, please verify your email address
          by clicking the button below.
        </p>

        <table role="presentation" cellpadding="0" cellspacing="0" style="margin: 0 auto 20px;">
          <tr>
            <td align="center"
                style="border-radius:10px;background-color:#5B23FF;box-shadow:0 4px 18px rgba(91,35,255,0.36);">
              <a href="{{verificationUrl}}" target="_blank"
                 style="display:inline-block;padding:15px 48px;color:#ffffff;font-size:15px;font-weight:600;text-decoration:none;letter-spacing:0.2px;font-family:'DM Sans',-apple-system,sans-serif;border-radius:10px;">
                Verify Email Address
              </a>
            </td>
          </tr>
        </table>

        <p class="expires">This link will expire in <strong>{{expiresIn}}</strong>.</p>

        <hr class="divider" />

        <p class="fallback">
          Button not working? Copy and paste this link into your browser:<br/>
          <a href="{{verificationUrl}}">{{verificationUrl}}</a>
        </p>
      </div>

      <!-- Footer -->
      <div class="footer">
        <p>&copy; {{currentYear}} ID Verification Platform. All rights reserved.</p>
        <p>If you did not create an account, you can safely ignore this email.</p>
      </div>

    </div>
  </div>
</body>
</html>
